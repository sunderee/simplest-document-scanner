name: example
description: "Sample application showcasing the use of simplest_document_scanner plugin"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: ">=3.10.0 <4.0.0"
  flutter: '>=3.38.2'

dependencies:
  flutter:
    sdk: flutter
  simplest_document_scanner:
    path: ../

dev_dependencies:
  flutter_lints: ^6.0.0
  flutter_test:
    sdk: flutter
  
flutter:
  uses-material-design: true